interface:
  display_name: "A-Share Stock Analysis"
  short_description: "Analyze A-share data, hotspots, stocks, holdings, signals, and risk-aware suggestions."
  default_prompt: "Use $a-share-stock-analysis to validate my A-share data or trade signals, find market hotspots, and produce risk-aware market, stock, portfolio, or signal-audit outputs."
policy:
  allow_implicit_invocation: true
